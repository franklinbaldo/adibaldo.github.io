\documentclass[11pt,twoside,openright]{book}

% ── Página ──
\usepackage[
  paperwidth=15.24cm,
  paperheight=22.86cm,
  inner=22mm,
  outer=13mm,
  top=19mm,
  bottom=19mm
]{geometry}

% ── Fontes (apenas texlive) ──
\usepackage{fontspec}
\setmainfont{TeX Gyre Termes}
\setsansfont{TeX Gyre Heros}
\setmonofont{TeX Gyre Cursor}

% ── Idioma ──
\usepackage[brazil]{babel}
\usepackage[utf8]{inputenc}

% ── Tipografia ──
\setlength{\parindent}{1.5em}
\setlength{\parskip}{0pt}
\usepackage{indentfirst}
\usepackage{microtype}

% ── Drop cap ──
\usepackage{lettrine}
\renewcommand{\LettrineFontHook}{\fontspec{TeX Gyre Termes}\bfseries}

% ── Cabeçalho e rodapé ──
\usepackage{fancyhdr}
\usepackage{xcolor}
\definecolor{gray}{rgb}{0.6,0.6,0.6}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[C]{\small\textcolor{gray}{Alfarrábios do Adi}}
\fancyfoot[C]{\small\textcolor{gray}{\thepage}}
\renewcommand{\headrulewidth}{0pt}
\fancypagestyle{plain}{\fancyhf{}\fancyfoot[C]{\small\textcolor{gray}{\thepage}}}

% ── Hiperlinks ──
\usepackage[hidelinks]{hyperref}

% ── Imagens ──
\usepackage{graphicx}

% ── Capítulos ──
\usepackage{titlesec}
\titleformat{\chapter}[display]
  {\normalfont\huge\bfseries}{\chaptertitlename\ \thechapter}{20pt}{\Huge}
\titlespacing*{\chapter}{0pt}{30pt}{20pt}

% ────────────────────────────────────────────
\begin{document}

% ── Página de título ──
\begin{titlepage}
\thispagestyle{empty}
\vspace*{\fill}
\begin{center}
  {\fontsize{32}{38}\selectfont\bfseries Alfarrábios do Adi}\\[16pt]
  {\large\itshape Memórias e Causos do Interior do Brasil}\\[32pt]
  {\Large Adi Baldo}\\[48pt]
  {\normalsize 2026}
\end{center}
\vspace*{\fill}
\end{titlepage}

% ── Página de direitos ──
\newpage
\thispagestyle{empty}
\vspace*{\fill}
\begin{flushleft}
{\small
© 2026 Adi Baldo. Todos os direitos reservados.\\[8pt]
ISBN: \_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\\[8pt]
Impresso no Brasil
}
\end{flushleft}
\vspace*{\fill}

% ── Sumário ──
\tableofcontents
\cleardoublepage

% ── Conteúdo (gerado pelo Pandoc) ──
$body$

% ── Colofão ──
\cleardoublepage
\thispagestyle{empty}
\vspace*{\fill}
\begin{center}
{\small\itshape
Adi Baldo nasceu em Barão de Cotegipe, Rio Grande do Sul.\\
Advogado, memorialista e contador de causos do interior do Brasil.
}
\end{center}
\vspace*{\fill}

\end{document}
